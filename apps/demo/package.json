{
  "name": "@wc-rask/demo",
  "version": "1.0.0",
  "private": true,
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.html --packages external --sourcemap=linked --experimentalCss --outdir ./dist --external lit --target browser --plugins bun-plugin-html --root ./src",
    "postbuild": "bun run build:declaration",
    "build:declaration": "tsc --project tsconfig.types.json",
  },
  "dependencies": {},
  "devDependencies": {
    "bun-plugin-html": "^2.2.4",
  },
}
